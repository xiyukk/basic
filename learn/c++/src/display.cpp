#include<iostream>
#include<string>
// #include<opencv2/core.hpp>
// #include<opencv2/highgui.hpp>

int main(int args, char** argv){
    // std::string filename = "../pic/R-C.jpeg";
    // if (args >= 2){
    //     filename = argv[1];
    //     std::cout << "Image file: " << filename << std::endl;
    // }

    // cv::Mat image = cv::imread(filename, cv::IMREAD_COLOR);
    // if (image.empty()){
    //     std::cout << "please input an image file path." << std::endl;
    //     return 0;
    // }

    // std::cout << "Image size is: " << image.size() << std::endl;
    // std::cout << "current opencv_version is: " << CV_VERSION << std::endl;

    // cv::namedWindow("image", cv::WINDOW_NORMAL);
    // cv::imshow("image", image);
    // cv::waitKey(0);
    // cv::destroyAllWindows();

    return 0;
}